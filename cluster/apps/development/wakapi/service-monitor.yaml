---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: wakapi-service-monitor
  namespace: development
  labels:
    app: wakapi-service-wakapi
spec:
  selector:
    matchLabels:
      app: wakapi-service-wakapi
  endpoints:
    - port: http
      path: '/api/metrics'
      interval: 30s
