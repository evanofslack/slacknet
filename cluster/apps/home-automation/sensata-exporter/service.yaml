---
kind: Service
apiVersion: v1
metadata:
  name: sensata-exporter-service
  labels:
    app: sensata-exporter
    prometheus: monitoring
spec:
  selector:
    app: mosquitto-exporter
  ports:
    - name: metrics
      port: 8000
