---
kind: Service
apiVersion: v1
metadata:
  name: mosquitto-exporter-service
  labels:
    app: mosquitto-exporter
    prometheus: monitoring
spec:
  selector:
    app: mosquitto-exporter
  ports:
    - name: metrics
      port: 9234
