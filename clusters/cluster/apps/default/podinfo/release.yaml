apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: podinfo
  namespace: default
spec:
  releaseName: podinfo
  chart:
    spec:
      chart: podinfo
      sourceRef:
        version: 6.8.0
        kind: HelmRepository
        name: podinfo
        namespace: flux-system
  interval: 60m
  install:
    remediation:
      retries: 3
  values:
